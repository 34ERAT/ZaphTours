import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#f35b29",
    },
    secondary: {
      main: "#eeeeee",
    },
    background: {
      default: "#eeeeee",
    },
  },
});
